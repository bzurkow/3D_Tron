import React from 'react';
import {expect} from 'chai';
import {shallow} from 'enzyme';
import {spy} from 'sinon';

// import Landing from '../../../../browser/components/Landing';

// describe('|----- FRONTEND Landing Component -----|', () => {
// 	let root, joinGameSpy;
// 	beforeEach('create component', () => {
// 		joinGameSpy = spy();
// 		root = shallow(<Landing joinGame={joinGameSpy} />);
// 	});
//
// 	it('Name input is originally empty', () => {
// 		expect(root.find('input[id="name-box"]').length).to.have.length(1);
// 	});

	// it('has proper initial state', () => {
	// 	expect(group.state()).to.be.deep.equal({
	// 		red: 0,
	// 		green: 0,
	// 		blue : 0
	// 	});
  // });
// });
